/**
 * Scout-agent — FASE 2 (sectie 10: bouwen na week 2).
 *
 * Volgt outliers van tracked_accounts, decodeert hook en structuur, en schrijft
 * kandidaat-heuristieken. Kandidaten worden pas actief nadat de Retro-agent ze
 * met eigen data bevestigt — daarom staan ze hier bewust op 'candidate'.
 *
 * Nog niet geimplementeerd. Deze module bestaat om de fasering expliciet te
 * maken en om de plek vast te leggen waar de implementatie landt.
 */
export async function runScoutAgent(): Promise<never> {
  throw new Error('Scout-agent is fase 2 en nog niet geïmplementeerd (sectie 10).');
}
